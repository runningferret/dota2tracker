require 'steam_helper'

describe SteamHelper do

    describe "#initialize" do
        it "accepts an api key" do
            helper = described_class.new( :an_api_key )
            helper.api_key.should == :an_api_key
        end
    end
end
